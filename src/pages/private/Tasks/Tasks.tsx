import { FC } from 'react';
import Navbar from '../../layouts/Navbar';

const Tasks: FC = () => {
	return (
		<div>
			<Navbar />
			{'Tasks'}
			<h1>Tareas</h1>
		</div>
	);
};

export default Tasks;
